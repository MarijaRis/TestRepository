function User(name, picture, album) {
    this.name = name;
    this.picture = picture;
    this.album = album;
    this.biography = biography;
    this.stories = stories;
    this.friends = friends;
    this.userId = function(){

          var random =  Math.floor(Math.random() * 500);

          for(user = 0; user <= 500; user++);

    }
}